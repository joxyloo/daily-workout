<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.user.myapplication">

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="Daily Workout"
        android:roundIcon="@drawable/logo"
        android:supportsRtl="true"

        android:theme="@style/AppTheme">
        <activity android:name=".splashscreen"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar"
            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".CountDown" />
        <activity android:name=".Description" />
        <activity android:name=".MainActivity" />
        <activity android:name=".WelcomeActivity" />
        <activity android:name=".ProfilActivity" />
        <activity android:name=".StartProgramActivity" />
        <activity android:name=".pilihLatihan" />
        <activity android:name=".TutorialActivity" />
        <activity android:name=".CreateProfil" />
        <activity android:name=".ReadProfil" />
        <activity android:name=".UpdateProfil" />
        <activity android:name=".MyProfilActivity" />
        <activity android:name=".AlarmActivity" />

        <receiver android:name=".AlarmReceiver" />
    </application>

</manifest>